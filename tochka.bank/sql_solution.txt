// task 3. Написать какой оптимальный индекс и какой покрывающий.
SELECT * FROM employee WHERE sex = 'm' AND salary > 300000 AND age = 20 ORDER BY created_at

Оптимальный индекс делается только для age потому что пол это пол таблицы, а зарплата диапазонный.
Покрывающий для всех полей в WHERE

// task 4. Храним информацию о книгах (название, автор, ISBN, кому выданы, когда выданы) и о пользователях (ФИО). 
// Составить таблицы и написать запрос чтобы получить книги, которые кто-то читает и их читателей

author
    id bigint
    name varchar
    birthday int
    
author_book
    id_author foreigh key (author.id)
    id_book foreigh key (book.id)

book
    id bigint
    name varchar
    isbn int
    reserve_date timestamptz
    reader_id foreigh key (user.id) null
    
user
    id bigint
    name varchar
    

select user.name, book.name
from book
inner join user.id = book.reader_id