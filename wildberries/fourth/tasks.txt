// 1 Может ли работать такая функция

func main() {
    d := add(3)(5)
    fmt.Println(d)
}

// 2 Нужно вывести символы английского алфавита
// - каждая третья буква должна заменяться и вместо нее выводиться индекс
// - каждая пятая буква должна заменяться и вместо нее выводится буква русского алфавита согласно индексу
// - каждая пятнадцатая - выводить 15. // 1 раз

-- 3 Вопрос:
--У нас есть две таблицы в базе данных интернет-магазина:

CREATE TABLE users (
                       id INT PRIMARY KEY,
                       name VARCHAR(100),
                       email VARCHAR(100)
);

CREATE TABLE orders (
                        id INT PRIMARY KEY,
                        user_id INT REFERENCES users(id),
                        amount DECIMAL(10,2),
                        created_at DATE
);

--Задача:
--Напишите SQL-запрос, который выведет:
--1) Имена пользователей
--2) Количество их заказов
--3) Общую сумму всех их заказов
--4) Только для пользователей, сделавших более 3 заказов
--5) Отсортировать по общей сумме (от большей к меньшей)// Напишите код, которая выводит алфавит от a до z.
