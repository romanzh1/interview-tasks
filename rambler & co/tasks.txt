
Архитектурные задачки:
1. Предположим у нас есть 4 страницы данных и мы не можем одновременно их загрузить в оперативную память.
Каждая страница содержит символы, которые нужно записать в итоговый 1 файл, но в отсортированном виде.
Каким образом можно решить эту задачу?


// Есть функция получения данных из "базы".
// Необходимо придумать вариант как нам правильно читать данные из бд
// Данные мы запрашиваем часто и много, тут для примера используем просто цикл, который пытается получить данные

package main

import (
  "fmt"
  "time"
  "sync"
)

type User struct {
  ID   int
  Name string
}

var (
  dbHits int
)

func getUserInfoFromDB(id int) User {
  time.Sleep(100 * time.Millisecond)
  dbHits++
  return User{ID: id, Name: fmt.Sprintf("User-%d", id), }
}

func main() {
  var wg sync.WaitGroup

  for i := 0; i < 5; i++ {
    wg.Add(1)
    go func(i int) {
      defer wg.Done()
      user, ok := getUserInfoFromCache(i)
    }(42)
  }
  wg.Wait()
  fmt.Println("DB hits:", dbHits)
}